<UserControl x:Class="UsbComposite.Views.ConnectDeviceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UsbComposite.Views"
        xmlns:helpers="clr-namespace:UsbComposite.Helpers"
        mc:Ignorable="d"
         >

    <UserControl.Resources>
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter" />

        <SolidColorBrush x:Key="DefaultForeground" Color="Black"/>
        <SolidColorBrush x:Key="DefaultBackground" Color="White"/>
        <SolidColorBrush x:Key="DefaultBorder" Color="#808080"/>
        <SolidColorBrush x:Key="AccentBlueBackground" Color="#FF4C61EC"/>
        <SolidColorBrush x:Key="AccentWhiteForeground" Color="White"/>
        <SolidColorBrush x:Key="HoverBackground" Color="White"/>
        <SolidColorBrush x:Key="HoverBorder" Color="black"/>
        <SolidColorBrush x:Key="ComboBoxActiveBorder" Color="Black"/>

        <SolidColorBrush x:Key="DisabledBackground" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="DisabledForeground" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="DisabledBorder" Color="#C0C0C0"/>

        <SolidColorBrush x:Key="ComboBoxHoverBackground" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="ComboBoxSelectedBackground" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ComboBoxDropDownBackground" Color="White"/>
        <SolidColorBrush x:Key="ComboBoxDropDownBorder" Color="#CCCCCC"/>

        <Style x:Key="CustomButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DefaultBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="7">
                            <ContentPresenter x:Name="ButtonContent"
                                      Margin="{TemplateBinding Padding}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource HoverBackground}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource HoverBorder}"/>
                                <Setter Property="Foreground" Value="{StaticResource DefaultForeground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentBlueBackground}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentBlueBackground}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentWhiteForeground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource DisabledBackground}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource DisabledBorder}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForeground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DefaultBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}"
                        CornerRadius="0"
                        SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ComboBoxHoverBackground}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBackground}"/>
                                <Setter Property="Foreground" Value="{StaticResource DefaultForeground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ComboBoxSelectedBackground}"/>
                                <Setter Property="Foreground" Value="{StaticResource DefaultForeground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource DisabledForeground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomComboBoxStyle" TargetType="ComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter
                                    Margin="2,0,0,0"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    IsHitTestVisible="False" />

                                    <ToggleButton Grid.Column="1"
                                    x:Name="PART_ToggleButton"
                                    Focusable="False"
                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    ClickMode="Press"
                                    Background="Transparent"
                                    BorderThickness="0">
                                        <Path x:Name="Arrow"
                                        Data="M 0 0 L 4 4 L 8 0 Z"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Fill="Black"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                  MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Background="White"
                                    BorderBrush="Gray"
                                    BorderThickness="1"
                                    CornerRadius="4"/>
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#FFE5E5E5"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FFAAAAAA"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="DarkGray"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    <Viewbox Stretch="Uniform">
        <Grid  Margin="5"  Width="1300" Height="800"   > 
                    <!-- 5 cột -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="16*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 3 hàng: trên và dưới nhỏ gọn -->
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="5*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="20*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="3*" />
                        <RowDefinition Height="*" />
                        <!-- Hàng dưới -->
                    </Grid.RowDefinitions>


                    <!-- Bạn có thể đặt gì đó vào hàng 0 -->
                    
            <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"  
                BorderBrush="#FF172976"
             CornerRadius="10" Background="White"
                   >
                <TextBlock Text="🔌 Chọn thiết bị để kết nối" 
               FontSize="40" FontWeight="Bold" 
               HorizontalAlignment="Center" 
                            VerticalAlignment="Center" 
                           />
            </Border>
            



                <!-- Kết nối CDC  Background="#2C3E50"> Grid.ColumnSpan="3"  -->
                    <Border Grid.Row="3" Grid.Column="3" CornerRadius="10" Background="White" >
                        <GroupBox   
                   BorderThickness="0" Foreground="Black"   FontSize ="18">
                            <Grid >
                                <!-- Định nghĩa 5 hàng -->
                                <Grid.RowDefinitions>
                            <RowDefinition Height="5*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="20*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="10*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                                <!-- Định nghĩa 6 cột (để linh hoạt chia 2 hoặc 3 cột) -->
                                <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="*"/>
                           
                        </Grid.ColumnDefinitions>

                                <!-- Hàng 0: Header -->
                                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" >
                                    <TextBlock Text="Comport"  HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       FontWeight="Bold" FontSize ="24"/>
                                </Border>
                                
                        <GroupBox  Grid.Row="2" Grid.Column="1" 
                                       Header="Configuration"  Background="White" FontSize="18" FontFamily="Segoe UI Semibold" 
                               >
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="5*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="5*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="1" Grid.Column="1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Text="Com" HorizontalAlignment="Left" VerticalAlignment="Bottom"  />
                                        <ComboBox Grid.Row="1" x:Name="cmbComPorts" 
                                          Style="{StaticResource CustomComboBoxStyle}" 
                                      Height="auto" Width="auto"
                                      ItemsSource="{Binding CdcVM.AvailablePorts}"
                                      SelectedItem="{Binding CdcVM.SelectedPort}" SelectionChanged="CmbComPorts_SelectionChanged" />


                                    </Grid>
                                </Border>

                                <Border Grid.Row="1" Grid.Column="3">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="Baud Rate" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                                        <ComboBox  Grid.Row="1" x:Name="cmbBaudRate" Width="Auto" Height="Auto" 
                                    Style="{StaticResource CustomComboBoxStyle}" 
                                    ItemsSource="{Binding CdcVM.BaudRateOptions}"
                                    SelectedItem="{Binding CdcVM.SelectedBaudRate}"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center" >
                                        </ComboBox>
                                    </Grid>
                                </Border>
                                
                                
                                <Border Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3">
                                    <Grid >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="8*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border  Grid.Column="0">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="Data bits" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                                                <ComboBox Grid.Row="1" x:Name="cmbDataBits" Width="Auto" Height="Auto" 
                                          Style="{StaticResource CustomComboBoxStyle}" 
                                          ItemsSource="{Binding CdcVM.DataBitsOptions}"
                                          SelectedItem="{Binding CdcVM.SelectedDataBits}">
                                                </ComboBox>

                                            </Grid>
                                        </Border>
                                        <Border  Grid.Column="2">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="Parity" HorizontalAlignment="Left" VerticalAlignment="Bottom" />

                                                <ComboBox  Grid.Row="1" Style="{StaticResource CustomComboBoxStyle}" 
                                              x:Name="cmbParity"
                                              ItemsSource="{Binding CdcVM.ParityOptions}"
                                              SelectedItem="{Binding CdcVM.SelectedParity}"
                                              Width="Auto" Height="Auto"/>

                                            </Grid>
                                        </Border>
                                        <Border  Grid.Column="4">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="Stop bits" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                                                <ComboBox Grid.Row="1" x:Name="cmbStopBits" Width="Auto" Height="Auto" 
                                          ItemsSource="{Binding CdcVM.StopBitsOptions}"
                                          SelectedItem="{Binding CdcVM.SelectedStopBits}"
                                          Style="{StaticResource CustomComboBoxStyle}" >
                                                </ComboBox>

                                            </Grid>
                                        </Border>

                                    </Grid>
                                    
                                </Border>
                                
                                
                            </Grid>
                        </GroupBox>

                        <GroupBox  Grid.Row="4" Grid.Column="1" 
                                       Header="Kết nối"  Background="White" FontSize="18" FontFamily="Segoe UI Semibold" >
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="5*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="1" Grid.Column="1">
                                    <Button Content="Open COM"
                            Command="{Binding CdcVM.OpenComCommand}"
                            IsEnabled="{Binding CdcVM.IsSerialOpen, Converter={StaticResource InverseBoolConverter}}"
                            Width="Auto" Height="Auto" 
                            Style="{StaticResource CustomButtonStyle}"/>
                                </Border>

                                <Border Grid.Row="1" Grid.Column="3">
                                    <Button Content="Close COM"
                            Command="{Binding CdcVM.CloseComCommand}"
                            IsEnabled="{Binding CdcVM.IsSerialOpen}"
                            Width="Auto" Height="Auto"
                                Style="{StaticResource CustomButtonStyle}"/>
                                </Border>

                            </Grid>
                        </GroupBox>


                        <!-- 
                                <Border Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3">
                                    <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="Com" HorizontalAlignment="Left" VerticalAlignment="Bottom"  />
                                <ComboBox Grid.Row="1" x:Name="cmbComPorts" 
                                          Style="{StaticResource CustomComboBoxStyle}" 
                                      Height="auto" Width="auto"
                                      ItemsSource="{Binding CdcVM.AvailablePorts}"
                                      SelectedItem="{Binding CdcVM.SelectedPort}" SelectionChanged="CmbComPorts_SelectionChanged" />
                                    </Grid>
                                </Border>


                                <Border Grid.Row="2" Grid.Column="6" Grid.ColumnSpan="3" >
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Text="Baud Rate" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>

                                        <ComboBox  Grid.Row="1" x:Name="cmbBaudRate" Width="Auto" Height="Auto" 
                                    Style="{StaticResource CustomComboBoxStyle}" 
                                    ItemsSource="{Binding CdcVM.BaudRateOptions}"
                                    SelectedItem="{Binding CdcVM.SelectedBaudRate}"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center" >
                                        </ComboBox>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" >
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Text="Data bits" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                                        <ComboBox Grid.Row="1" x:Name="cmbDataBits" Width="Auto" Height="Auto" 
                                          Style="{StaticResource CustomComboBoxStyle}" 
                                          ItemsSource="{Binding CdcVM.DataBitsOptions}"
                                          SelectedItem="{Binding CdcVM.SelectedDataBits}">
                                        </ComboBox>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="4" Grid.Column="4" Grid.ColumnSpan="2"  >
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
    
                                        <TextBlock Grid.Row="0" Text="Parity" HorizontalAlignment="Left" VerticalAlignment="Bottom" />

                                        <ComboBox  Grid.Row="1" Style="{StaticResource CustomComboBoxStyle}" 
                                              x:Name="cmbParity"
                                              ItemsSource="{Binding CdcVM.ParityOptions}"
                                              SelectedItem="{Binding CdcVM.SelectedParity}"
                                              Width="Auto" Height="Auto"/>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="4" Grid.Column="7" Grid.ColumnSpan="2" >
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>

                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="Stop bits" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                                        <ComboBox Grid.Row="1" x:Name="cmbStopBits" Width="Auto" Height="Auto" 
                                          ItemsSource="{Binding CdcVM.StopBitsOptions}"
                                          SelectedItem="{Binding CdcVM.SelectedStopBits}"
                                          Style="{StaticResource CustomComboBoxStyle}" >
                                        </ComboBox>
                                    </Grid>
                                </Border>




                                <Border Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="3" >
                                    <Button Content="Open COM"
                            Command="{Binding CdcVM.OpenComCommand}"
                            IsEnabled="{Binding CdcVM.IsSerialOpen, Converter={StaticResource InverseBoolConverter}}"
                            Width="Auto" Height="Auto" 
                            Style="{StaticResource CustomButtonStyle}"/>
                                </Border>

                                <Border Grid.Row="6" Grid.Column="6" Grid.ColumnSpan="3" >
                                    <Button Content="Close COM"
                            Command="{Binding CdcVM.CloseComCommand}"
                            IsEnabled="{Binding CdcVM.IsSerialOpen}"
                            Width="Auto" Height="Auto"
                                Style="{StaticResource CustomButtonStyle}"/>
                                </Border> -->
                            </Grid>
                        </GroupBox>
                    </Border>



                <!---USB HID -->
                    <Border Grid.Row="3" Grid.Column="1"  Grid.RowSpan="3"
                BorderBrush="#FF172976"
             CornerRadius="10" Background="White" >

                        <Grid   DataContext="{Binding CanVM}" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="5*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="20*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="20*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="10*"/>
                                <RowDefinition Height="*"/>

                            </Grid.RowDefinitions>

                    <Grid  Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="DiagnosticCan"  HorizontalAlignment="Center" VerticalAlignment="Center" 
                                         FontSize ="24" FontWeight="Bold"/>

                        <Button Grid.Row="0" Grid.Column="3" Content="Default diagnostic" 
                                Command="{Binding Config.SetDefaultFilterValuesCommand}"
                                 Style="{StaticResource CustomButtonStyle}" Margin="7" FontSize="16" FontFamily="Segoe UI Semibold"/>

                    </Grid>

                    <GroupBox  Grid.Row="2" Grid.Column="1" 
                                       Header="Configuration"  Background="White" FontSize="18" FontFamily="Segoe UI Semibold" 
                               >
                                <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="5*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="5*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="1" Grid.Column="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Label  Grid.Row="0" Grid.Column="0"  Content="Baud Rate (kbit/s)"  />
                                    <ComboBox   Grid.Row="1" 
                                            ItemsSource="{Binding Config.BaudRateOptions}"
                                            SelectedItem="{Binding Config.SelectedBaudRate}" 
                                                Style="{StaticResource CustomComboBoxStyle}" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="3">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Label  Grid.Row="0" Grid.Column="0"  Content="Sample point"  />
                                    <TextBox Grid.Row="1"  Text="{Binding Config.SamplePointText, 
                                        Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                             LostFocus="SamplePointTextBox_LostFocus"/>
                                    
                                </Grid>
                            </Border>
                                    
                            <Border Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"  >

                                <Grid x:Name="BitTimingBar" HorizontalAlignment="Stretch" SizeChanged="BitTimingBar_SizeChanged">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding Config.SyncSegWidth}" />
                                            <ColumnDefinition Width="{Binding Config.Seg1Width}" />
                                            <ColumnDefinition Width="{Binding Config.Seg2Width}" />
                                        </Grid.ColumnDefinitions>

                                        <Rectangle Grid.Column="0" Fill="Gray" />
                                        <Rectangle Grid.Column="1" Fill="Green" />
                                        <Rectangle Grid.Column="2" Fill="Orange" />
                                    </Grid>

                            </Border>
                        </Grid>
                    </GroupBox>
                            
                            
                    <GroupBox  Grid.Row="6" Grid.Column="1" 
                                       Header="Kết nối"  Background="White" FontSize="18" FontFamily="Segoe UI Semibold" >
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="5*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="1" Grid.Column="1">
                                <Button Content="Kết nối CAN"
                                                Style="{StaticResource CustomButtonStyle}"
                                                Command="{Binding ConnectCanCommand}"
                                                IsEnabled="{Binding IsDisconnected}"/>
                                
                            </Border>

                            <Border Grid.Row="1" Grid.Column="3">
                                <Button Content="Ngắt kết nối CAN"
                                                Style="{StaticResource CustomButtonStyle}"
                                                Command="{Binding DisconnectCanCommand}"
                                                IsEnabled="{Binding IsConnected}"/>
                            </Border>

                        </Grid>
                    </GroupBox>
                    
                    <GroupBox  Grid.Row="4" Grid.Column="1" 
                                       Header="Acceptance Filter"  Background="White" FontSize="18" FontFamily="Segoe UI Semibold" >
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="4*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="6*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="1" Grid.Column="1">
                                <RadioButton Content="Standard (11-bit ID)"
                                            IsChecked="{Binding Config.IsStandardIdFilter, Mode=TwoWay}"
                                             HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontWeight="Bold" />
                            </Border>

                            <Border Grid.Row="1" Grid.Column="3">
                                <RadioButton Content="Extended (29-bit ID)"
                                             IsChecked="{Binding Config.IsExtendedIdFilter, Mode=TwoWay}" 
                                             HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                         
                            </Border>

                            <Border Grid.Row="3" Grid.Column="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock   Grid.Row="0"  Text="From ID(Hex):" VerticalAlignment="Center"/>
                                    <TextBox  Grid.Row="1"  Text="{Binding Config.FilterFromId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  />
                                </Grid>
                            </Border>

                            <Border Grid.Row="3" Grid.Column="3" >
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="To ID(Hex):" VerticalAlignment="Center" Margin="0,5,0,0"/>
                                    <TextBox Grid.Row="1"  Text="{Binding Config.FilterToId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>
                            </Border>
                        </Grid>
                    </GroupBox> 


                    <!--
                            <GroupBox Header="Cấu hình CAN Protocol" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10"
          HorizontalAlignment="Stretch" VerticalAlignment="Top">
                                <StackPanel Margin="5">

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                        <Label Content="Baud Rate (kbit/s):" />
                                        <ComboBox Margin="15,0,0,0" Width="90" ItemsSource="{Binding Config.BaudRateOptions}"
                                            SelectedItem="{Binding Config.SelectedBaudRate}" />
                                    </StackPanel>

                                    <GroupBox Header="CAN ID Filter" Margin="0,5,0,0">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                                <RadioButton Content="Standard (11-bit ID)"
             IsChecked="{Binding Config.IsStandardIdFilter, Mode=TwoWay}" />
                                                <RadioButton Content="Extended (29-bit ID)"
             IsChecked="{Binding Config.IsExtendedIdFilter, Mode=TwoWay}" Margin="15,0,0,0"/>
                                            </StackPanel>

                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="From ID(Hex):" VerticalAlignment="Center"/>
                                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Config.FilterFromId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" Margin="5,0"/>


                                                <TextBlock Grid.Row="1" Grid.Column="0" Text="To ID(Hex):" VerticalAlignment="Center" Margin="0,5,0,0"/>
                                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Config.FilterToId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" Margin="5,5,0,0"/>
                                            </Grid>

                                            <Button Content="Default Values" Command="{Binding Config.SetDefaultFilterValuesCommand}" Margin="0,5,0,0" HorizontalAlignment="Left" Padding="10,3"/>
                                        </StackPanel>
                                    </GroupBox>

                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <Button Content="Kết nối CAN"
                                                Style="{StaticResource CustomButtonStyle}"
                                                Command="{Binding ConnectCanCommand}"
                                                IsEnabled="{Binding IsDisconnected}"/>
                                        <Button Content="Ngắt kết nối CAN"
                                                Style="{StaticResource CustomButtonStyle}"
                                                Command="{Binding DisconnectCanCommand}"
                                                IsEnabled="{Binding IsConnected}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>
                            
                            -->



                        </Grid>
                    </Border>  
                    <!-- Hàng dưới cùng -->        

                           
        <TextBlock Text="⚠️ Vui lòng kết nối thiết bị trước khi thao tác" 
               Grid.Row="7" Grid.Column="0"  Grid.ColumnSpan="5" 
               FontSize="20" Foreground="#FFD62152" 
               HorizontalAlignment="Center"
               Margin="0,10,0,0" />



                    <Border Grid.Row="5" Grid.Column="3"   
                BorderBrush="#FF172976"
             CornerRadius="10" Background="White"
                   >
                        <TextBlock Text=" Cấu hình I2C" 
               FontSize="20" FontWeight="Bold" 
               HorizontalAlignment="Center" 
                            VerticalAlignment="Top" 
                                   
                           />
                    </Border>


                </Grid>

    </Viewbox>
            
</UserControl>
